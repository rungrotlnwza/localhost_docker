````markdown
# 📦 localhost_docker

โปรเจกต์นี้ใช้ Docker Compose เพื่อสร้างระบบฐานข้อมูล **MariaDB** พร้อมเครื่องมือจัดการ **phpMyAdmin** บนเครื่อง **Localhost**
ออกแบบให้ใช้งานได้ร่วมกับทุกโปรเจกต์ในเครื่องอย่างต่อเนื่อง โดยไม่ต้องรันคำสั่งทุกครั้งที่เปิดเครื่อง

---

## 🚀 วิธีเริ่มต้นใช้งาน (ครั้งแรกเท่านั้น)

1. **ติดตั้ง Docker และ Docker Desktop**
2. เปิดเทอร์มินัลในโฟลเดอร์โปรเจกต์นี้
3. รันคำสั่ง:
   ```bash
   docker-compose up -d
   ```
4. ตั้งค่าใน Docker Desktop ให้ container เริ่มทำงานอัตโนมัติ (ดูขั้นตอนด้านล่าง)

---

## ⚙️ ตั้งค่าให้ Container ทำงานอัตโนมัติ

เพื่อให้ระบบฐานข้อมูลพร้อมใช้งานทุกครั้งที่เปิดเครื่อง:

1. เปิด **Docker Desktop**
2. ไปที่เมนู **Containers**
3. เลือก container ชื่อ `mariadb` และ `phpmyadmin`
4. เปิดสวิตช์ **"Start container when Docker Desktop starts"**

> ✅ ทำเพียงครั้งเดียว Container จะเริ่มทำงานอัตโนมัติทุกครั้งที่เปิด Docker

---

## 📁 โครงสร้างไฟล์

| ไฟล์                | รายละเอียด                          |
|---------------------|--------------------------------------|
| `docker-compose.yml`| กำหนดบริการฐานข้อมูลและ phpMyAdmin |
| `README.md`         | คู่มือการใช้งาน                      |

---

## 💡 การใช้งานร่วมกับโปรเจกต์อื่น

หลังจากตั้งค่าแล้ว ทุกโปรเจกต์ในเครื่องของคุณสามารถเชื่อมต่อฐานข้อมูลได้ทันที:

```text
Host: localhost
Port: 3306
User: root
Password: iflovethenus
Database: mydb
```

> ไม่ต้องรันคำสั่งใด ๆ ใหม่ เพียงแค่เปิด Docker Desktop แล้วใช้ได้เลย

---

## 🌐 phpMyAdmin

หากต้องการดูหรือจัดการฐานข้อมูลผ่านเบราว์เซอร์ เข้าได้ที่:

```
http://localhost:8080
```

---

## 🧊 ข้อมูลถาวร (Persistent Volume)

- ข้อมูลจะถูกเก็บใน volume ชื่อ `mariadb_data`
- ข้อมูลจะไม่สูญหายแม้จะปิดเครื่องหรือหยุด container

---